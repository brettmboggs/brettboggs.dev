# Handoff: get Nightjar onto TestFlight from this Mac

You are a Claude Code session running locally on Brett's MacBook, inside the
`brettboggs.dev` repo checked out at `~/Desktop/nightjar` on branch
`claude/hush-sleep-app-redesign-fgpbes`. A cloud session built the app and
could not go further because signing and App Store Connect need this Mac and
Brett's Apple account. Read `app/README.md` and the root `CLAUDE.md` first.

## State right now

- The app is `app/Nightjar.xcodeproj`, one target, generated by
  `python3 app/tools/make_project.py`. It builds and runs on Brett's iPhone
  from Xcode (Team "Brett Boggs", automatic signing, bundle id
  `dev.brettboggs.nightjar`). `app/tools/update.sh` pulls and installs.
- There is NO App Store Connect app record for `dev.brettboggs.nightjar`
  yet, and the bundle id is NOT registered under Certificates, IDs & Profiles.
  Only the old `dev.brettboggs.hush` ids exist.
- App Store Connect shows a banner: the Apple Developer Program License
  Agreement was updated and the Account Holder must accept it. Nothing
  uploads until that is accepted.
- PR #1 on GitHub merges this branch to `main`. `.github/workflows/testflight.yml`
  would upload from CI but has no secrets; ignore it unless Brett asks.

## Do this, in order, and tell Brett exactly what to click when it needs him

1. **Agreement.** Open https://developer.apple.com/account in the browser and
   have Brett accept the updated license agreement. Confirm the banner on
   https://appstoreconnect.apple.com/apps is gone.
2. **Register the bundle id.** Prefer doing it from the command line so it
   cannot be mistyped: build once to the device with automatic signing
   (`xcodebuild -project app/Nightjar.xcodeproj -scheme Nightjar
   -destination 'generic/platform=iOS' -allowProvisioningUpdates build`
   registers the App ID), or use
   https://developer.apple.com/account/resources/identifiers/add/bundleId
   › App IDs › App › Description "Nightjar", explicit `dev.brettboggs.nightjar`.
3. **Create the app record.** https://appstoreconnect.apple.com/apps › + ›
   New App › iOS, Name "Nightjar", English (U.S.), Bundle ID
   `dev.brettboggs.nightjar`, SKU `nightjar`, Full Access.
4. **Archive and upload.** From `~/Desktop/nightjar`:
   ```
   python3 app/tools/make_project.py
   xcodebuild archive -project app/Nightjar.xcodeproj -scheme Nightjar \
     -configuration Release -destination 'generic/platform=iOS' \
     -archivePath build/Nightjar.xcarchive -allowProvisioningUpdates \
     CURRENT_PROJECT_VERSION=1
   ```
   Then write an ExportOptions.plist with `method` = `app-store-connect`,
   `destination` = `upload`, `signingStyle` = `automatic`, `teamID` = the
   ten-character team from `app/Local.xcconfig` or the project, and run
   `xcodebuild -exportArchive -archivePath build/Nightjar.xcarchive
   -exportOptionsPlist ExportOptions.plist -exportPath build/export
   -allowProvisioningUpdates`. This uses the Apple ID signed into Xcode.
   If it asks for an App Store Connect API key instead, fall back to Xcode:
   Product › Archive › Distribute App › TestFlight Internal Only.
   Bump `CURRENT_PROJECT_VERSION` by one on every upload.
5. **Testers.** App Store Connect › Nightjar › TestFlight › Internal Testing ›
   create a group, add Brett. Then External Testing › create a group with a
   Public Link for everyone else (first external build gets Beta App Review).
6. **Make it automatic.** In Xcode: Product › Xcode Cloud › Create Workflow.
   Connect the GitHub repo, archive on pushes to `main`, deploy to TestFlight
   internal group. Free tier is 25 compute hours a month. This replaces the
   GitHub Actions workflow; delete `.github/workflows/testflight.yml` once
   Xcode Cloud works.
7. **Merge PR #1** so `main` has the app and the site pages the app links to
   (`/nightjar/privacy/`, `/nightjar/terms/`).

## Rules from the repo you must keep

- No mention of AI, Claude, or assisted authorship anywhere: commits, PR
  text, code, App Store metadata. No Co-Authored-By trailers.
- No em-dashes in copy. Stoic, short.
- Do not spend money. The developer membership is already paid.
- Xcode edits `app/Nightjar.xcodeproj` when opened; never hand-edit it,
  regenerate with `python3 app/tools/make_project.py` and commit that.
- `app/Local.xcconfig` holds the team id and is gitignored. Leave it.

## If the build breaks

`python3 app/tools/check_swift.py` and `python3 app/tools/verify_project.py`
are the offline checks. The engine in `app/Nightjar/Audio/` is the part Brett
wants kept; the rest was rebuilt on 2026-09-05 and has had one device build.
